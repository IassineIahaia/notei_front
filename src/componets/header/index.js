import{Navbar, Container} from 'rbx';
import logoImage from '../../assets/images/logo.jpg';

function Header() {
    return (
        <header>
            <h1>Header</h1>
        </header>
    );
}

export default Header;